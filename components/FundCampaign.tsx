import { useWeb3Contract, useMoralis } from "react-moralis";

export default function FundCampaign() {
  // const {runContractFunction: contributeToCampaign} = useWeb3Contract({abi:, contractAddress:, functionName:, params:{}, msgValue:,})
  return <div>Hi from FundCampaign</div>;
}
